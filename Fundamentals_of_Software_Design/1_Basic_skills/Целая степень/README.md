3 Задача «Целая степень»

Возвести число в целую степень (используя минимальное число умножений).

```python
def power(x: float, n: int) -> float:
  ...
```

```c
double power(double x, int n)
{
    /* ... */
}
```

**Подсказка**: использовать двоичное представление числа $n$.
